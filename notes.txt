the knowedge graph can be uploaded to canvas and so can everything else, add support for that as well , 

Canvas integration can be a future improvement -> secret key for integration given after contract